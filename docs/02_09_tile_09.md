# Tile 9: Heavy Industrial Expanse

Dense but industrial. Smokestacks, cargo lines, heavy machinery. Port + channel + refinery systems dominate.

- **Density:** Ultra High (infrastructure density)
- **Form:** Heavy industry. Port landmass + engineered shoreline dominate the right/bottom-right; infrastructure density peaks here.
- **Atmosphere:** reflects the **lateral ambiance gradient** appropriate to this tile’s position and must not contradict the global progression
- **Key Elements**
  - **R1 composition map (layout authority):**
    - Use the Tile 9 R1 composition map as the PRIMARY encoding of framing/sky-budget rules.
    - Match its: sky budget line, horizon line height, skyline envelope slope, reading orientation arrow (bottom-left → top-right),
      edge-safe zones, seam blending zones, and primary anchor zone placement.
  - **Composition lock (DO NOT DRIFT):**
    - Dominant read is a **strong diagonal** running **bottom-left → top-right** (matches the panorama’s right-side bias).
    - **Skyline envelope (Tile 9 signature):** a sloped envelope **higher on the LEFT, descending toward the RIGHT** (per R1).
    - The primary water body is a **navigable industrial river/channel** that occupies the **bottom-right quadrant**, hugs the **right edge**, and **recedes into the horizon**. Avoid a hard “L” turn that re-orients the scene.
    - The left bank (land side) is a continuous industrial frontage: docks, quays, straight seawalls, containment edges, and hard angular shoreline geometry.
    - **Seam blending strips (hard rule):** left/right seam blending zones must remain low-uniqueness repeatable industrial texture only
      (pipes, roof-fields, container repetition, service yards). No hero objects touching seams; no clipped “signature” structures at edges.
  - **Harbor wedge geometry (required):**
    - Engineered shoreline forms a dominant diagonal **land wedge** rising **bottom-right → mid-right/top-right (↗)**.
    - Foreground + right side are dominated by **PORT LANDMASS** (container yard grids, quays, cranes, warehouses, service roads).
    - Add 2–4 quay fingers / dock basins / pier projections to sell “port geometry” (not just a shoreline line).
  - **Engineered shoreline REQUIRED:** Land–water boundary must read as an engineered, industrial shoreline: predominantly straight, angular, and artificial, formed by docks, harbors, and containment structures. Organic, eroded, or natural coastlines are not allowed.
  - **Port + shipping (scale anchors):**
    - Include realistic port infrastructure: piers, cranes, mooring zones, container stacks, service roads, cargo trucks, empty container handlers (ECH).
    - Include **at least one utilitarian cargo ship/tanker** (correct scale). Prefer a ship presence near the **lower-right** without making it a “hero” centerpiece.
  - Ultra-dense heavy industry: refineries/petrochemical plants, stacks, pipe forests, conveyors, warehouses, power/utility infrastructure, railyards and track fans.
  - **Visibility / clarity rule (anti-blur):**
    - Keep visibility high: **no fog veil, no milky smog blur, no volumetric whiteout blanket**.
    - Atmospheric perspective is allowed only as subtle distance desaturation; **edges remain crisp**.
  - **Color/grade constraint (anti-blue-drift):**
    - After Tile 5, warmth must not “return” as a global grade, but Tile 9 must also **avoid cyan/blue cast** drift.
    - Target palette: **desaturated polluted steel + dirty beige/soot-gray particulate** (cool-neutral overall, not “blue-hour”).
    - Any warmth may exist only as **tiny utilitarian work-light accents** (optional), never as a global regrade.
  - **Light-direction enforcement:** key light from the **left**; primary cast shadows fall to the **right**.
  - Wind direction to right (affecting chimney smoke)
- **Forbidden Motifs**
  - Nature
  - Landmark buildings
  - Commercial CBD / office skyline: no corporate glass tower clusters, no “downtown” high-rise identity.
    Any tall verticals should read as industrial (stacks, cranes, silos, gantries), not prestige skyscrapers.
  - Natural coastline reads (beaches/coves/eroded shore); the shoreline must remain engineered/industrial
  - Residential streets
  - Sun prohibited (see _Global Constraints / Lighting_).

## References

Tile 9 is generated with **R1 + ruler** references. References are conditioning inputs only; they must not be copied as content.

- **Reference R1 (Layout): Tile 9 R1 composition map — REQUIRED**
  - **Transfer allowed:** layout/framing rules (sky budget, horizon height, skyline envelope slope, reading orientation, seam zones, anchor zone).
  - **Transfer forbidden:** literal content copying.

- **Reference A (Ruler): Tile 5 ruler image**
  - **Transfer allowed:** camera/framing physics (horizon/vanishing-height read), telephoto compression feel, vertical “pressure”, sky-budget discipline (do not open sky), midground scale.
  - **Transfer forbidden:** any Tile 5 content identity (no skyline motifs, no landmark silhouettes, no central-axis logic, no sun disk).

- **Optional seam anchor:** Tile 8 RIGHT EDGE crop (when available)
  - **Transfer allowed:** right-edge seam continuity language (silhouette height profile + edge texture density), not full composition.
  - **Transfer forbidden:** importing Tile 8 content into the center.

- **Precedence**
  - **R1 controls layout. Reference A controls framing physics.**
  - If any reference conflicts with `docs/01_core_canonical.md` or this tile spec (light direction, sun prohibition, wind direction, maximum haze mandate), the reference must be ignored for that attribute.

- **Suggested upload order:** Reference A.

## Generation (Single Pass)

Tile 9 is generated in **one pass** using a single prompt.

- **Uploads / Inputs:** Reference R1 (Tile 9 layout) — REQUIRED; Reference A (Tile 5 ruler) — REQUIRED. Optional seam anchor as needed.
- **Primary objective:** lock framing/scale + engineered shoreline + heavy-industry layering, while achieving the Tile 9 haze endpoint.
- **Atmosphere constraint:** keep Tile 9 as the strongest haze endpoint, but avoid full whiteout. Midground remains readable with soft edges; far background is strongly collapsed into milky smog (ghost silhouettes only, no crisp edges).
- **Output artifact:** `tile-09_final`.

## Active Calibration Layer

- **Framing stability (Tile 5 as ruler):**
  - Treat vertical drift as camera pitch drift; do not vertically recenter to fit smokestacks or sun.
  - Crop/clip when elements pressure the top. If sky feels too open, add industrial mass, stacked midground infrastructure, or haze inside the distance volume—do **not** open more sky.
  - Prevent push-in reads by avoiding a single foreground yard/track fan; reduce near-field dominance and emphasize layered repetition into depth.
- **Clarity enforcement (anti-blur):**
  - If distance becomes too washed out, restore contrast in the midground and keep far background readable (do not rely on fog/smog blur to hide structure).
  - Maintain crisp edges; use distance desaturation only (no milky smog blanket).
